export interface ContactMethod {
  icon: string;
  title: string;
  description: string;
  details: string;
  cta: string;
  action: string;
}

export interface FAQ {
  question: string;
  answer: string;
}
